#include "main.h"

/**
 * main - A function that  checks for a digit (0 through 9).
 *
 * Return: 1 if its a digit.
 * Return: 0 overwise.
 */

int _isdigit(int c)
{
	int i;

	if (i = 0 || i <= 9)
		return (1);
	else
		return (0);
}
