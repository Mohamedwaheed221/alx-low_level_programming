#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	char latter
	 for (letter = 'a' ; letter < z ; ++letter)
	 {
		 putchar('letter')
	 }
	return (0);
}
